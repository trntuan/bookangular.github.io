// <button [style]="colorH"  (click)="tagetClick()">đổi màu nen</button>
// <input type="text" [(ngModel)]="valueP" >

// <button (click)="view = !view">
//     <ng-container *ngIf="view"> view </ng-container>
//     <ng-container *ngIf="!view"> hide </ng-container>

// tagetClick(): void {
//     let a = Math.random() * 10000;
//     this.colorH = `background-color: #${a.toFixed().toString()}`;
//   }
// view: boolean = true;
// textBtn: string = "nhấn vào để xem";

// <!-- [text]="textBtn"   let j = index-->
// <!-- (valueEmitter)="addlisten($event)" -->

// [style]="colorH" 